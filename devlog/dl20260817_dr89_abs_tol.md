# DR-89: The Absolute-Tolerance Floor, Jury-Diagnosed and Fixed Same Day

**Date:** 2026-08-17
**Purpose:** Record the three-voice jury on the startup convergence
crawl and the same-day fix
**Module:** sparse, fem/kernel
**Rounds:** `tmp/ai_exchange/convergence_regression_jury.md` (jury),
implementation on Christian's go

## The question and Christian's objection

Fresh-start tapestack3d crawled at 10–26 magnetic iterates/step with
oscillating stalls at −87…−97 dB and Δt collapse, where the weekend
configuration ran fine. Christian rejected the proposed constant-jc
control ("the current configuration DID work on the weekend") and
called a jury. His objection held: all three voices exonerated the
weekend AND killed the constant-jc experiment as unnecessary.

## Verdict (Grok + blind fresh Claude + reconciler; Codex quota-benched)

The magnetic linear solve exits at STRUMPACK's library default
`abs_tol = 1e-10`, which BELFEM had never overridden. At startup the
outer ‖b‖ ≈ 1.4e-2, so that floor sits at −85 dB — exactly the observed
stall entry — while the 1e-11 relative nonlinear target needs −110.
Convergence happened only on GMRES overshoot lottery wins. On the
weekend ‖b‖ was ~10³ larger and the identical floor sat harmlessly at
−115 dB. The subtle binary delta: the old refinement path OVERSHOT to
machine precision when it converged (the deck's "−156 dB observed");
PREC_GMRES stops AT tolerance — the linear-accuracy contract silently
dropped five decades under a deck tuned to the old behavior. The blind
voice supplied the transmission (the iterate tails are the relaxation
schedule crawling against the floor, reproduced line-by-line) and the
likely cause of DR-88's tiny pivots (ρ_HTS ≈ 0 blocks at startup —
checkable for free at the knee). Unfold/BDF/postproc/trim suspects all
killed for this regime by three independent arguments.

The reconciler's own pre-registered floor measurement survived; his
attribution of the floor to factor accuracy was corrected by both other
voices (exit test, not accuracy — the occasional 1e-16 finals prove it).

## The fix (same day)

`absolute tolerance` is now a full deck key: validated setter, parser,
copy ctor, synchronize (12 uints + 3 reals). STRUMPACK ALWAYS applies
it — default 1e-14, because the library's 1e-10 was the defect; PETSc
applies it only when deck-stated (its own 1e-50 default is benign).
Input-contract pair updated; three new unit tests plus the copy-walk
extension, ctest 14/14. The S9 by-catch (the headroom gate's
direct-solvers-are-exact premise, falsified by this defect) is corrected
in place: the exemption stands on the new abs_tol-governs-the-endgame
rationale, with a revisit note if the default is ever loosened.

Falsifiable prediction for the live gate — the warm restart from the
100 ms memdump: ≤6 iterates/step, endgame deterministically below
−115 dB, Δt recovering, ~3–4× wall-rate improvement.
