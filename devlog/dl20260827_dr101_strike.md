# DR-101 Strike: PARDISO Indexing-Base Leak

**Date:** 2026-08-27
**Scope:** Records-only closure of DR-101 after Christian's strike ruling.
**Status:** Closed by ruling; no source edits.

## Summary

Christian ruled "Strike!" after reviewing DR-101. The live row was closed on the existing red-to-green evidence.

## Evidence

- Defect: `PARDISO::initialize` leaked the matrix into Fortran/1-based indexing after symbolic factorization, violating the `SpMatrix` assembly/operator contract and aborting debug `examples/sidecoating`.
- Corrected severity: release `operator()` uses the base-aware `position()` path, so no silent Jacobian corruption was shown; the actual severity was a debug-fatal contract violation plus a loud release hazard for callers that require Cpp base.
- Fix: `src/sparse/cl_SolverPARDISO.cpp` backs up the entry base after wrapper initialization, sets PARDISO's Fortran base for parameter setup and symbolic factorization, then restores the entry base after the status check, matching the `solve()` contract.
- Gates: pre-fix debug sidecoating hit the `SpMatrix::operator()` indexing assert after thermal Picard 1; post-fix the same debug deck ran 4+ BDF timesteps with no assert; release `make check-fast` passed 9/9.
- Audit residues were not kept as blockers: inverted `mParameters(1)` doc comment, vector-solve symbolic rerun / `gMemoryPointers`, and an optional PARDISO lock-in test.

## Changes

- Removed DR-101 from the live register and run-gate strike queue.
- Added DR-101 to the closed archive.
- Indexed this devlog.

No source code was touched.
