# Debt Register

**Purpose:** the live set of open items across all campaigns, with the blocking-1.0 column as
the release lens. Retired rows move to [debt_register_closed.md](debt_register_closed.md). The
canonical examples cited below now live there.

## Freeze lens

Every open row now carries a second tag beside its `[CODE]`/`[RULING]`/`[RUN]` kind:

- **`[F]`** — settle **before the design freeze**. Reserved for rows whose resolution changes a
  **user-visible contract**: a default, an input key's meaning, which examples ship, or a
  documented convention. After the freeze these become expensive or breaking to change.
- **`[P]`** — post-freeze. Internal defects, performance, test coverage, verification runs.
  A freeze locks design, not bug fixing, so these are safe to carry.
- **`[W]`** — watch. Latent hazards with no current trigger and no reachable caller; no action
  is owed unless something starts firing.

**The `[F]` list is EMPTY**

Everything else open is `[P]` (8 rows, recounted 2026-09-05 — **DR-157 filed 2026-09-05 while salvaging the removed test-hardening plan: the nightly job never received the `-DUSE_GASMODELS=ON` Christian ruled for it on 2026-08-30, so the gas-model tests have never run in CI**; previous recount 2026-09-04 — **DR-39 STRUCK and archived 2026-09-04: its four harvested circuit tests RAN green in `make check` and the demo executable was then deleted, discharging (a) by deletion per the 2026-08-30 ruling; (b) re-homed to the ngspice plan's Phase 6 with its three constraints carried across. The diode Newton has its first transient gate; it converged with omega = 1, worst case 11 iterations**; same-day recount — **DR-156 filed 2026-09-04: the ParMETIS / PT-Scotch wiring landed and is green at np 2/4 but its benchmark is owed; D5 ( self-loops corrupting `METIS_NodeNDP` on the PETSc+metis path ) fixed and gated the same day**; previous recount 2026-09-02 — **DR-155 filed 2026-09-01 as by-catch of DR-131's Darwin gate: `matvec_csc`'s OpenMP array reduction puts each worker's private copy of the whole result vector on the thread stack, crashing above 262,144 rows ( = 2 MiB / 8 B, measured to the byte ). A new row rather than a widening of DR-131, per the extend-don't-branch policy's own carve-out for a genuinely separate subsystem — this is `src/sparse`, not the plugin contract. **FIXED the same day by REMOVING the parallelism ( default-OFF `BELFEM_OMP` token ) rather than repairing it — build gate owed. Widened first, after a Codex+Grok jury round on its plan: the row's own 'USE_MKL=ON bypasses the kernel' scope guard was FALSE ( the 2-argument `SpMatrix::multiply` has no MKL branch and crashes MKL builds on the eigen path ), its Linux cliff figure was unearned, its 'measured to the byte' overstated a 1152-byte bracket, and its planned fix would have produced silently wrong matvecs. Widened rather than branched on Christian's ruling — one DR-155 sweep.** DR-131 itself was retagged `[RUN-BLOCKED]`→`[RUN]` the same session once its gate finally RAN on a Mac, and WIDENED to carry the four example decks, which had never received the fix the templates got and could neither build nor load their plugins on Darwin**; previous recount 2026-08-31 — **DR-147 STRUCK and archived 2026-08-31 ( Christian's ruling ): filed, unblocked, fixed, audited in four rounds and GATE-VERIFIED BY EXECUTION in a single session. The "layering block" that had parked it since 2026-08-29 was never one — the resolver body needs only `file_exists` and `gBelfemDataPath`, both already on every module's include path, so the search order moved into `io/filetools` with NO `src/` CMake change and `material::data_file()` became a forwarder. Two of the row's own claims were WRONG and were corrected on the way out: three plugin keys resolved TWO ways, not three, and the fix cost zero caller churn rather than "touches every caller". Both plan-stage auditors refuted the proposed gate — a unit test on a new function cannot run red on a tree where that function does not exist — which is why this landed the suite's FIRST MODULE target and FIRST `dlopen`-based test. 17/17 new cases confirmed present in the built binaries by name and EXECUTED, not inferred from the suite result; both halves of the red-then-green ran in one binary, the contamination control doubling as the pre-fix path. Scope widened per extend-don't-branch to three by-catch defects ( two leaked `dlopen` handles, one error path naming a file the loader never opened ) plus a pre-existing doc defect: an unset `$BELFEM_DATA` does NOT leave only step 1 on an installed tree** — **DR-116 STRUCK and archived 2026-08-31 ( Christian's ruling, DR-42/DR-49 pattern ): all five HDF5 writers now create with fixed-width file types and write through the NATIVE memory type, and the round's own by-catch was larger than the row — a genuine reader defect in `load_bool_from_file` ( FILE type used as the `H5Dread` MEMORY type, overrunning a one-byte `hbool_t` by 3 bytes, reproduced with a canary before fixing ). The row's originally proposed gate was found to be a NULL GATE that already passed on the unfixed tree, and was replaced. `make` and the io ctest suite are the surviving residue** — **DR-147 FIXED and retagged `[CODE][W]` → `[RUN][P]` 2026-08-31, its `make check` gate owed.** The layering block that had parked it dissolved on inspection — the resolver body needs only `file_exists` and `gBelfemDataPath`, both already on every module's include path — so the search order moved into `io/filetools` with no `src/` CMake change and `material::data_file()` became a forwarder. Two of the row's own claims were WRONG and are corrected in it: three plugin keys resolved **two** ways, not three, and the fix cost zero caller churn rather than "touches every caller". Both audit rounds refuted the proposed gate — a unit test on a new function cannot run red where that function does not exist — which is why this landed the suite's **first MODULE target and first `dlopen`-based test**. Scope widened per extend-don't-branch to three by-catch defects (two leaked `dlopen` handles, one error path naming a file the loader never opened) plus a pre-existing doc defect Codex found: an unset `$BELFEM_DATA` does NOT leave only step 1 on an installed tree — **DR-120 STRUCK and archived 2026-08-31 ( Christian's ruling ) once its own gate RAN GREEN in seven cases the same session: one control ( the unmodified deck accepted, 12 completed timesteps ) and six setup aborts inside `Controller::set_params()`, each naming its key, covering both `max iterations` keys, the `max ≥ min` cross-check, both windows and `coupling factor`. The discriminator is the 0-vs-−1 pair on `watchdog window` — the documented disable sentinel passes where the illegal value aborts — so the checks separate legal from illegal rather than rejecting the key. The pre-fix silent `uint` wrap was NOT re-measured: it is a static argument from `get_int` plus the `uint` members, and reproducing it needs a rebuild with the checks reverted. The row was never a pending fix — the code had been in the tree and in both binaries since 2026-08-28, and its `file:line` citations were found stale by ~210 lines on the day it was struck, already re-anchored once by the 2026-08-30 sweep** — **DR-135, DR-136 and DR-137 all STRUCK and archived 2026-08-31, closing the whole background-field / periodic-Dirichlet cluster: DR-135 on evidence ( the reroute RAN on a purpose-built crossing-symmetry deck and DISCRIMINATES — 16 of its 17 condensed sources lie outside the pinned sideset, so the pre-fix path would have lost 16 constraints — with the unconstructible field-equivalence clause waived ), DR-136 on a design ruling ( the background field is imposed WEAKLY by design; the strong variant would force removal of the automatic gauge pins and a phi-domain gradient for a handful of dofs, and the reasoning now lives in `doc/input_file_reference.md` §13 rather than only in the register ), DR-137 as accepted exposure ( ceiling-before-watchdog costs burnt iterations, not a wrong answer; NEVER EXECUTED, and its scope is wider than the row said — `iterate_magnetic()` carries the same ordering ).** DR-154 filed, fixed AND STRUCK 2026-08-30, all in the session that found it: every tesla token in `unit_to_si` carried VOLT's dimension exponents, so `che**DR-154 filed, fixed AND STRUCK 2026-08-30, all in the session that found it: every tesla token in `unit_to_si` carried VOLT's dimension exponents, so `check_unit( x, "T" )` could not separate the two and a `voltage` condition accepted `5 T` as 5 V. Corrected on Christian's ruling, and its gate RAN — `make check` 17/17 in the claude sandbox, with two of the four new StringTools cases discriminating against the pre-fix table** — DR-139 STRUCK and archived 2026-08-30 once its gate RAN GREEN on a purpose-built **non-MKL** debug tree ( `/opt/scls/debug` reference BLAS/LAPACK — an MKL build cannot settle a leak question, its kernels read uninitialized padding by design ): no `SpMatrix` in any leak record, all 34 definitely-lost blocks being OpenMPI/PMIx `MPI_Init` allocations. No positive control was built, so the result is absence-of-evidence supported by the run resolving BELFEM allocation sites to `file:line` throughout. **That same run's by-catch was larger than the row: 22 BELFEM invalid-read/write contexts exposed a live heap buffer overflow in `ElectricalCircuit::compute_adjacency()`**, fixed the same night with its own gate still owed — DR-138 STRUCK and archived 2026-08-30 ( Christian's DR-42/DR-49 single-run exception ): its unit gate had RUN red-then-green as pre-registered, and **the owed coupled reject-retry A/B is WAIVED with the row**. The session that struck it first found that A/B MIS-SCOPED — the controller's reject/retry sequence on the circuit object is call-for-call what the unit test already runs — and then probe-EXECUTED the one thing the coupled path genuinely adds, the FEM leg: it carries DR-138's exact shape and is safe **only** because `set_current_and_voltage` is accept-only; `shift_back()` gives `mIn` no protection at all, so any future path writing `mIn` on a non-accepted attempt re-creates the defect silently. The coupled deck's reject-retry ran six times without abort, but no rejection-free reference twin exists for it, so the coupled level stays REVIEWED, NOT VERIFIED — **DR-111 STRUCK 2026-08-30 ( Christian's ruling, DR-42/DR-49 pattern ) after its fix landed in the CONSUMER rather than the factory: the row's own root cause was wrong twice over ( the 2D winding is UNCONDITIONAL, not orientation-dependent, and it is FORCED by the edge-index/`mS` contract, so no permutation is both CCW and sign-neutral ) and so was its severity ( for an all-thin-shell thermal domain `M`/`K`/`f` are uniformly negated and the temperatures are UNCHANGED — the DR-06 cold-spot claim does not apply ). `|det J|` in a QUAD4TS-only `dV_quad4ts`, plus a QUAD4TS-only Pipette measure; `make check` green, fix confirmed present by symbol, and a coupled 2D thin-shell quench deck ran 750+ steps. Its residual is named and real: that run was a RELEASE binary with the guard compiled OUT, so an assertions-ON run is the only thing that would make it verified — DR-129 STRUCK and archived 2026-08-30 ( Christian's ruling ) after ALL THREE of its run gates ran green the same day: debug `make check` 17/17 with `GhostElementContract [ OK ]`, release ( `USE_DEBUG=OFF`, `-O2 -DNDEBUG` ) `make check` 17/17 with the same test — release-ness proven from an ASSERT-only string being ABSENT in the linked binary rather than from the cache, after a first attempt at that gate was caught running a debug binary — and a release coupled h-ɸ/T smoke that carried the R8 `ThermalFactory` construction site at startup then ran 523 converged coupled timesteps with zero DR-129-class failures. The smoke was closed early and deliberately: that site executes ONCE at startup, so the deck's remaining hours could add no evidence. This strike clears the register's only P1 row** — DR-106 STRUCK and archived 2026-08-30 ( Christian's ruling ) after BOTH its gates ran the same afternoon: the rebuilt binary's `tape_quench_usermat` restart hit `-9` twice at step 791 and the new in-wrapper ICNTL(14) ladder recovered both ( 30→60→120→240, step then converged, zero soft-fail boxes ), and `check-fast` ran 15 of 15 suites green incl. `sparse` — the archived row carries the log citations; DR-112 STRUCK 2026-08-30 after its three-case restart gate ran ( current dump resumes, triple-less dump refused on 4/4 ranks, `restart : false` starts fresh ); note `make check` does not reach `load_memdump`, so that row needed a hand-run gate — DR-145 STRUCK 2026-08-29 after `make check` passed and the rebuilt binary was confirmed to carry and pass its cases, recounted mechanically 2026-08-29 — DR-140 STRUCK and archived 2026-08-29 once `make check` RAN GREEN: its two symmetry regression tests had only ever been hand-linked, and the row itself said a scratch link cannot verify build wiring; both are now confirmed compiled into the built `test_sparse` with `sparse` in the `check` dependency list. Struck on the reading that triangle extraction is a FEATURE in its own todo file rather than this row's residue — DR-144 WIDENED rather than branched 2026-08-29: its code-audit round turned up two further defects in the same subsystem ( a stale `mK` on a worker, and a solve bail that is silent with soft-fail off ) and both went into that row as G10/G11 under the extend-don't-branch policy. They were briefly filed as DR-149/DR-150 and WITHDRAWN the same hour — DR-149 collided with an ID a parallel session had already used and withdrawn, which is the failure mode 'IDs are never reused' exists to prevent, and the scan that missed it had matched only row starts and not prose — **DR-149 was filed and WITHDRAWN the same day on Christian's ruling that the register is inflating one ID per finding; its evidence folded into DR-112, whose loader contract it belongs to, and its work into `todo/closed/restart_timestep_consistency.md`. Standing policy now, recorded in the rules above: extend the row being worked, branch only for a genuinely separate subsystem.** DR-143 STRUCK and archived 2026-08-29 after BOTH its gates ran green: `make check` including the new `SolverLifecycle.MUMPSPoolRecyclesFreedSlots` test, and the tapestack3d rerun which reached 43 conditioning calls with zero pool-exhaustion aborts where the old allocator died on the 8th; DR-118 STRUCK and archived 2026-08-29 once its owed demo run was CONDUCTED ( sandbox, four cases incl. two the old code could not survive ); DR-121 and DR-123 STRUCK and archived 2026-08-29 by Christian's ruling, both having already run their unit gates green with no residue named; DR-117 and DR-141 both STRUCK and archived 2026-08-29 after `make check` passed ( Christian ): DR-117's five new `SourceFunction` cases were confirmed compiled into the circuit binary and run 5/5 green rather than assumed from the suite result, and DR-141's gate WAS the clean build. Both had already been probe-verified in the same session — DR-117's row had been wrong twice over, about its own severity ( 'LATENT / sine unaffected': the offset setter back-computes the phase, so the sine was wrong by the same pi^2 ) and about its own proposed one-line repair ( which re-introduced a `0 * INFINITY` NaN at zero period ). DR-145 filed 2026-08-29 as by-catch of the paired DR-117/DR-141 fix round ( `std::fmod` keeps its dividend's sign, so square/triangle/sawtooth evaluate a negative normalized time at `t < time_offset()`; measured by probe, deliberately kept OUT of DR-117 because that fix is what restores the offset and makes it observable ), the same session that FIXED DR-117 and DR-141 and retagged both `[CODE]`->`[RUN]` with their `make check` gates owed — DR-117's filed severity ( 'LATENT / sine unaffected' ) and its filed one-line repair were BOTH refuted in that round; DR-141 filed 2026-08-29 as by-catch of the DR-139 code-audit round ( implicit-copy double-free hazard on `ElectricalCircuit`/`Solver`, flagged by both vendors ), the same session that FIXED DR-139 and retagged it `[CODE]`→`[RUN]` with its valgrind gate owed; DR-140 filed 2026-08-29 by the eigen/ARPACK session for the MUMPS symmetric-mode defect ( full matrix under SYM != 0 gets its off-diagonals summed twice; wrapper guard + IWG default flip landed, capability gap deliberately left open ); DR-127 STRUCK and archived 2026-08-29 after ALL its gates ran green: the certified exit fixed, code-audit-jury reconciled ( both vendors converged on the same two wrapper defects ), the jjc-noise deck reproduced as smooth, and its last unreachable branch — the `mResetThermal` return in `solve_thermal` — executed under a temporary forced-reset probe that retried at a halved Δt₂ without hanging, probe removed the same session; DR-138/DR-139 filed that day by the circuit-cluster round ( inductor `shift_back` current loss; `mMNA` destructor leak ), the same session that landed the DR-118/DR-121/DR-123/DR-124 fixes and retagged all four `[CODE]`→`[RUN]` with their run gates owed; previous mechanical recount 2026-08-28 — DR-137 filed 2026-08-28 from the DR-119 plan audit ( magnetic watchdog vs its own ceiling, Grok's flag ), same session in which DR-97's latch extension, DR-119's coupled clamp and DR-120's validations ( scope widened to watchdog/stall windows and `coupling factor` ) all LANDED, reviewed not verified, run gates surviving in their rows; DR-130 struck 2026-08-28 by Christian's ruling: `h_ghost`'s symmetric operator is intentional (the April 2026 symmetrization was deliberate per its own audit devlog), doc corrected and the pin assertion landed the same session with only its `check-fast` rerun owed; DR-132 struck 2026-08-28 by Christian's ruling: the plugin `dlopen` boundary stays a documentation-only contract, no version handshake for 1.0; DR-96 struck 2026-08-28 after both halves of its runtime gate ran and passed in the claude build sandbox; DR-135 FIXED and retagged `[CODE][W]`→`[RUN][P]` 2026-08-28: shared-helper pin landed after a three-round jury ending in unanimous approve, only the probe-deck gate remains; DR-134 struck 2026-08-28 once §19.7 and §20.3-20.7 of `dof_manager_usage_guide.md` were rewritten against the current tree, with the `maxwell_usage_guide.md` §8 and `contact_impedance_theory.md` sibling sites fixed in the same pass; DR-126 struck 2026-08-28 by Christian's ruling as superseded by the autopin feature (`04c33664`), its SideSet sibling residue split into DR-135; DR-88 struck 2026-08-28 as superseded by DR-126's demonstrated gauge singularity (DR-126 itself since closed by the autopins); DR-134 filed 2026-08-28 as DR-85's stale-doc residue; DR-95 struck 2026-08-28 after its comment demotion landed at all three sites; DR-93 struck 2026-08-28 after its current-impact claim was refuted; DR-92 struck 2026-08-28 after its production restart gate passed; DR-77 struck 2026-08-28 as a hardware-capacity limitation rather than software debt; the previous count had gone stale across the night's parallel sessions; DR-46 struck 2026-08-28 — fixture built and its `make check-fast` gate RUN green the same night, DR-129/DR-130 filed from its by-catch; DR-126/DR-127/DR-128 filed the same night by the jjc-noise round; DR-37 struck 2026-08-28 after both its gates ran; DR-115 through DR-118 added 2026-08-27, DR-114 added and struck the same day and now archived, DR-104 ruled a documented modeling choice and struck+archived 2026-08-27, DR-119/DR-120 added and DR-97 retagged `[F]`→`[P]` late 2026-08-27, DR-121 added same evening, DR-103 struck+archived 2026-08-27 with its order-2 residue split into DR-122, DR-123/DR-124 filed from the DR-115 fix audit, DR-115 itself struck the same night — fix gate-passed) or `[W]` (3 rows, recounted 2026-09-05 — **DR-158 filed 2026-09-05 from the same salvage: the Tier 2 `mpirun` is found on PATH and never checked against the MPI the binary was linked with; crash-not-silent, so watch only**; previous recount 2026-08-31 after DR-147 was fixed and retagged to `[P]`; previously 3 after DR-153 was struck the same day as DR-136; previous recount 2026-08-30 — **DR-151, DR-152 and DR-153 were all filed 2026-08-30 as by-catch of the DR-111 fix round, and two were settled the same day on Christian's rulings. DR-151 STRUCK, FIXED: the non-positive layer thickness is now refused at the parse site, naming the material, the stack and the value, instead of surfacing a phase later as a generic negative-volume abort. DR-152 STRUCK, no code change: the mesh checker never sees a mesh carrying thin-shell elements, so its corrupting swap table is unreachable by construction rather than merely untriggered. DR-153 STRUCK 2026-08-31 ( Christian's ruling ), FIXED: the orphan carried the ONLY copy of the March pyramid fix `d205680a` ( PYRA13/PYRA14 → `swap_pyra14` ), which was ported into the live kernel copy — confirmed by an independent derivation from the PYRA14 parameter coordinates — before both orphan files were deleted; its grep criterion now returns exactly one path** — DR-144 STRUCK DR-144 STRUCK and archived 2026-08-30 ( Christian's ruling ): all 11 steps landed once R12/R13 rode the DR-106 joint session, its lifecycle gates had run green 2026-08-29, and the 2026-08-30 rebuild compiled R12/R13 into the green suite; G10/G11 have no reachable in-tree trigger, so that is the row's maximum evidence — DR-90 STRUCK and archived 2026-08-29 by Christian's ruling: its gate was found INAPPLICABLE rather than merely blocked ( the magnetic field now runs a direct solver, so the rel-tol the A/B was meant to vary never reaches it ), the conditioning half was answered from surviving run logs without the deleted dump, and the stall class no longer reproduces — DR-147 filed 2026-08-29 as by-catch of the tape_quench plugin audit ( the three plugin `.so` paths resolve differently; blocked on a layering choice, deliberately kept out of the rename diff ); — DR-86 REFUTED and struck 2026-08-29 by Christian's ruling after a source trace showed the aura-lag mechanism has no write path: field writes are rank-0-only and `DofManager::solve` distributes the whole field vector, auras included, after EVERY iterate; DR-136 filed 2026-08-28 for the unfinished `BackgroundDirichlet` feature — deck-refused since 2026-08-11, but a deck is planned "soon" and the row is that work's checklist; DR-135 filed 2026-08-28 as DR-126's unaudited `SideSet::impose_dirichlet` sibling, then FIXED and retagged to `[RUN][P]` the same night; DR-75 struck 2026-08-28 after its runtime gates had passed; DR-122 added 2026-08-27), with no untagged rows remaining (DR-100 struck 2026-08-28 after both MPI production reproducers and `check-fast` passed; DR-85 struck 2026-08-28 by Christian's ruling with its A/B gate waived and its stale-doc twin split into DR-134; DR-94 struck 2026-08-28 after its documentation fix and design ruling were confirmed complete; DR-102 struck 2026-08-28 once the second half of its recorded fix was found missing from the tree and landed). Nothing in those two groups needs a
decision this week; they need time, which is the one thing the freeze does not take away.

**Maintenance (protocol §11):** a session that resolves or creates an open item updates this
register in the same session. IDs are stable and are never reused. **Prefer extending an existing row over filing a new one ( policy set 2026-08-29, Christian ):** when a session's own gate or audit turns up a finding in the same contract or subsystem as the row being worked, widen that row's scope cell and put the work in its plan, rather than branching a fresh ID. New rows are for genuinely separate subsystems. The register had been inflating one ID per finding.

What follows is the operating manual for the register. It is not history — the per-pass narrative
this preamble used to carry is in git, which is what git is for.

## How to read a row

A ~~struck ID and description~~ means the row is **retired** — no design/code/ruling work remains.
The status column stays unstruck because closure evidence, surviving gates, and waivers live
there. The strike itself is not verification evidence: **struck is not verified.**

When a row's description and status cell disagree, **the status cell is the verdict** on whether
the item is open — but a stale description is still currentness work, not permission to read past
it. A dead clause inside a still-live row is struck in place. **The row supersedes examples in this
preamble:** where an example and the row conflict, the row wins.

Writing `closed` in the status cell and striking the ID and description are **two separate
actions**. A row marked closed in status but never struck is live work for the next sweep.

## When to strike

Strike the ID and description only when the work is finished with **no residual**. A row stays live
when it is merely downgraded, part-done, or still waiting on a decision, a design question, or an
unwritten test. A residual living in another session or artifact is still a residual.

**Exception, Christian's call — the DR-42/DR-49 pattern:** when the *design* work is finished and
the residue is a single run, Christian may strike the row anyway. Record the exception; the gate
survives in the status column, and **struck is not verified** applies with full force. Christian
may instead retire the gate *with* the row — write that in the status cell, never infer it.

A static sweep may recommend a strike, or a by-design / already-fixed / nothing-actionable closure;
it does not issue the ruling. **"Pending run" is not a row a static sweep can close.**

When a row's amendment history outgrows human readability, **compact it** and say so in the row.
Git carries the generations; the register carries the current state.

## How a sweep gathers evidence

**For what the source does today, the evidence is the tree — never the plan and never the devlog.**
Verify on disk, not by inference. Rulings and waivers are exceptions: the recorded decision is the
evidence.

If the source still matches an unstruck row's claim, leave it. Classifying what rows are *waiting
on* from their status columns is a different pass from a currentness sweep — useful, but say which
one you ran, and never report one as the other.

A stale row is neither reliably pessimistic nor reliably optimistic. Sweeps have found debt already
paid, debt **larger** than recorded, and written claims that were simply false. If a claim is
refuted, rewrite what survives — it is often narrower and different. Do not close a false row and
lose the real defect underneath it.

Uncommitted tree state is not closure. Commit state can change a row's *argument* — unreferenced
dead code rather than abort-shadowed — without changing its severity.

A cleanup row that is ready to execute pins its exact change set. Before closing a plan, lift its
leftovers into this register. Detailed residuals stay in the owning plan; this register is not a
second copy of it.

## Closure evidence (protocol §11)

Use this ladder, weakest first, for register evidence: **AI agreement → single-voice static source
trace/review → standalone probe → clean compile/link → named suite gate → named
production/end-to-end run that can observe the row.** A strike does not move a row up this ladder;
only the status evidence does. Say **reviewed** for static-only work and **verified** only when an
executable gate passed.

**Agreement is the weakest tier.** In this register, it has repeatedly done no work: promote a
finding on a re-read of the tree, not on a vote. Flag single-raiser rows as such, and keep
confidence in a mechanical file:line trace separate from confidence in any arithmetic riding on it.

**Pick a gate that can observe the defect.** For the canonical residual-invariant Jacobian/history
case, use Newton-iteration count, not a physics-output comparison.

**A status cell records what a run did *not* cover** as well as what it did. A waived gate is named
there.

Honest closures can be something other than "the test went green": already fixed in tree, stale or
moot decision, by-design, or out-of-scope. Record which one — never dress a decision as run
evidence.

A core-controller termination change is not applied on a static finding. It takes plan+audit →
code+audit with both vendors, plus an A/B.

## Standing traps

1. **A grep-shaped absence is not a functional gap.** Prove the thing is unavailable, not merely
   absent from the one function you read; trace the alternative routes first. Canonical: DR-19:
   the connector record is deliberately absent from the shell record because the coating block
   types and wall-to-facet links ship by two other explicit channels. That false positive survived
   a jury round, a row edit, and two independent reads before the code refuted it. Retract a false
   positive in the row; do not quietly delete it.
2. **Follow the ID you cite.** A plausible ID copied forward without being followed is the same
   defect class this register exists to catch. The always-active `set_currents` guard that broke
   every parallel run is **`61bbfa11`** — not DR-24, the periodic free-cut `mPrescribedCurrents`
   row. That wrong citation stayed in this register for two passes.
3. **When a row names N sites, count them in source.** Canonical: DR-45 — three `INFO` sites, not
   two, with the Matrix-RHS `solve` overload still gated on `rank() == 0` after the commit that
   supposedly fixed it.
4. **A commit can pass its own acceptance test and still leave the neighborhood wrong** — an
   adjacent input-contract key, a comment, or a document. The acceptance test clears the change,
   not what sits beside it.
5. **A source comment that asserts as fact what every other artifact in the same commit correctly
   hedges is itself debt.** The register row is allowed to be the better-calibrated statement,
   and has been.

## Triage

Every live row waits on a **run**, a **ruling**, or **code work** — or a named mix. Classify before
planning: a ruling costs a sentence from Christian, runs batch together, and only the code-work
residue needs a full audit round.

Each live row carries that classification as a tag in its ID cell:

| tag | meaning |
|---|---|
| `[RUN]` | the only residue is executing a named gate. Inputs exist, no code, no decision — autopilot-ready |
| `[RUN-BLOCKED]` | a run gate whose inputs are gone or whose upstream is unfixed. **Not** autopilot-ready; the row says what died |
| `[RULING]` | needs a sentence from Christian — a strike, a commit, a scope call. No machine time |
| `[CODE]` | code remains, so it takes the standing plan+audit → code+audit round |
| `[MIXED]` | a named combination; the row states the order |

The tags are a **classification pass from the status columns as written** — not a currentness
sweep, and no source was re-verified to place them. Re-derive a tag whenever a row's status
changes; a tag is the cheapest thing in the row to let go stale.

**`[RUN-BLOCKED]` is the tag that earns its keep.** Three gates have already died because the
dump or the deck they named was deleted between the fix and the run. When a row's gate depends on
an artifact, name the artifact in the status cell and say it must be kept — see the evidence rule
above, and expect the default to be deletion.

| ID                          | area | severity | status | reproducer | blocking-1.0? |
|-----------------------------|---|---|---|---|---|
| DR-29 **[CODE]** **[P]**    | homology: coreduce worklist port (#1/#2a) + stale-adjacency cleanup (#10) — sequenced after rectification guards/tests | P2 | deferred — **confirmed open 2026-08-13, and #10 is more specific than recorded.** ~~The live path `coreduce_complexCCR()` → `pGeneralizedCocombine(p)`~~ **live-path clause corrected 2026-08-30: production is `coreduce_complexPellikkaGeneralized()` (`cl_MaxwellFactory.hpp:48` default + `mSuggestHomologies` hardcoded true `cl_CutFactory.hpp:91`), which re-enters `pCoreduce(p+1)` after each cocombine phase — CCR is opt-in.** `pGeneralizedCocombine` (body `:1119-1228`) is a single forward pass with an inner backward scan — no worklist, no queue, no neighbor re-enqueue. The Pellikka path `pCoreduce` (`cl_SimplicialComplex.hpp:338-400`) is the older full-rescan form. **#10 pinned:** the stale-adjacency cleanup blocks (`:1175-1196`) sit inside `if ( tNeighborIDs.size() > 0 )` (`:1175`), yet `remove_kcochainFromMap` runs unconditionally (`:1223-1224`) and only deletes the cochain and erases the map entry (`hpp:320-332`) — so when there is no valid neighbor the removed IDs stay referenced in adjacent boundary maps. **Three-way analysis round 2026-08-30 (Claude + Codex sol/xhigh + Grok 4.6/xhigh, blind jury, reconciled):** #10 confirmed as an elementary coreduction pair whose bookkeeping is skipped; a production-reachable abort cascade was constructed independently by both auditors (3-D, zero-neighbor skip leaves dead face in a tet boundary → later merges shrink it to a dead raw singleton → `pCoreduce` raw `size()==1` picks the dead ID → `Map::operator()` abort `hpp:369`, all build types) — order-dependent, the "every change breaks it" mechanism: safety today is by phase ordering only, unchecked and undocumented. **Refuted and must not be "fixed":** the Smith matrices from `createMatrixFromCoboundaryMap` are CORRECT — δ∘δ=0 makes the live-restriction exactly the retracted operator (Codex derivation, independently re-verified); do not mirror merges into (p−1) coboundary rows and do not switch the matrix source to boundary maps. Fix plan (design only, nothing touched): (0) debug invariant — live↔live row/column agreement + mD·mD=0; (1) #10 cleanup unconditional AND find()-guarded incl. the `(p+2)` walk `:1190-1194`; (2) `pCoreduce` pair test live-and-unit (Mrozek §4 ±1 check missing); (3) worklist port last, FIFO+membership set, dequeue revalidation, component-aware omit seeding — `pCocombine` is NOT a template (LIFO `Cell::pop`, sorting `unique()`, unguarded pop). Report for Gregory: session 2026-08-30, `devlog/dl20260830_dr29_coreduce_analysis.md`. **HANDED OFF 2026-08-30 (Christian): the report went to Gregory Giard, who develops this module and decides what happens to it. The row stays DEFERRED pending his call. The fix plan above is a proposal for him, not a queued task — no BELFEM-side session should self-start it, and the analysis does not need redoing.** **AI EDIT BAN 2026-08-31 (Christian + Gregory):** the cohomology core (`cl_Cohomology`, `cl_Homology`, `cl_SimplicialComplex`, `cl_Chain`, `cl_Cochain`, `fn_Smith`) is closed to AI edits regardless of session edit approval — `doc/ai_collaboration_protocol.md` §7.1. This row is read-and-report only; steps (0)-(3) are executable by Gregory, or under his named authorization, and by nobody else. | Betti/cut-count comparison; step-0 invariant green on annulus×3 algorithms + both periodic 3-D fixtures, debug AND release — gates apply only if Gregory adopts the plan | no |
| DR-71 **[MIXED]** **[W]**   | homology **the `mSuggestHomologies == false` branch of `CutFactory::compute_cohomologies` is unreachable and broken in two independent ways.** Found 2026-08-13 while building the DR-23 layer fixture. The flag is hardcoded `true` (`cl_CutFactory.hpp:91`, comment: "this needs to be turned on for the incidence matrix to work!"), so the `reduce_complex*` calls inside `if (!mSuggestHomologies)` never execute in production. When forced: (1) running reduce AND coreduce together zeroes the SNF input — reduce guts the chain side (all top cells omitted), coreduce guts the cochain side, and `createMatrixFromCoboundaryMap` then sees only zero maps, so every surviving 1-cochain becomes a "generator"; (2) `pGeneralizedCombine` carries an unbounded iterator walk (`cl_SimplicialComplex.cpp:873-878`): the `while` advances `it2` with no `end()` bound and the `if (it2 == tSimplicesMap.end()) continue;` below it is unreachable-as-guard — when no boundary simplex qualifies, `++it2` walks past `end()` (UB; observed as a 99%-CPU infinite loop). The sibling cocombine loop (`:1139-1148`) IS bounded (breaks at `begin()`), so the defect is one-sided. NOT triggered from any production path today. Per-symptom reproducers: **(1) reproduces on any well-formed mesh** — force `mSuggestHomologies = false` so reduce and coreduce both run, and the SNF input zeroes by composition; **(2) was observed only through the malformed complex** (the DR-23 `aComputeConnectivities = false` fixture bug, which empties every edge coboundary so no boundary simplex ever qualifies and `++it2` walks past `end()`) — on a well-formed complex the loop may happen to terminate, but the missing bound and the dead guard are structural either way. Options when touched: bound the loop like its sibling, or retire the dead branch outright. **SPLIT BY THE AI EDIT BAN 2026-08-31** (`doc/ai_collaboration_protocol.md` §7.1): defect (2), the unbounded `++it2` at `cl_SimplicialComplex.cpp:873-878`, is INSIDE the banned core and is Gregory's alone; defect (1) and the "retire the dead branch" option live in `cl_CutFactory.cpp:278-281,295-298,329-332`, which the ban does NOT cover. Do not treat this row as wholly closed to AI work, and do not treat the banned half as actionable | P3 | open — latent; no production path reaches it | (1): any mesh with `mSuggestHomologies` forced false; (2): the `tests/homology/` annulus built with `aComputeConnectivities = false`, then `reduce_complexCCR` | no |
| DR-105 **[MIXED]** **[P]**  | examples/deck-envelope: **the tape_quench deck is tuned and its quench-front abort has a landed remedy; three things are left — a long rerun, an unexplained parallel wall, and one physics ruling.** Split out of DR-02 at its closure ( Christian's ruling, 2026-08-24 ). The "µs-scale Δt contraction radius" it was filed for turned out to be a COLD-START artifact: the same deck that rejects a cold 50 µs first step grows into 20 µs steps once BDF5 history is built, so ramp-in matters and step size per se does not. The two physical drivers are the 100× Gaussian jc-suppression defect under n≈37 and the measured current table's first jump at t≈0.77 ms | P2 | open — **recipe applied, remedy landed, rerun owed.** T5 recipe ( `initial timestep : 0.002 ms`, `maximum timestep : 0.02 ms`, strumpack magnetic / petsc thermal, Anderson on, Newton via the 1e-4 switch, chi off ) came out of a six-variant matrix at equal 12-15 min wall budgets: T5 reached 0.954 ms and rode the cap with near-zero rejections, straight through the 0.77 ms wall, while T6 found the natural ceiling at ≈16-30 µs with rejected probes at 31 µs — capping just under it beats letting the PID hunt ( 4.8 vs 3.2 ms/hour ). **The serial 9.8 h discriminator settled the np=8 question and then hit a new defect:** serial ran to t = 9.14 ms at ~1.0 ms/h — 24× past the np=8 wall — so that wall is a parallel effect, still unexplained; the run then aborted at the quench front on `rho_piecewise`'s `n > 1` assert ( `powerlaws.hpp:780`; the floor-retry abort above it is `cl_FEM_Controller.cpp:2700-2706` ) inside the 90 K < T ≤ 92.5 K window, where the plugin's own fits have fallen back to 1.0 but `T_crit` keeps the superconducting branch alive. Debug-only guard; a release build would divide by zero in the flux-flow branch. **That abort now has a landed remedy:** the 2026-08-29 usermat round single-sourced `hts_Tcrit`, made the fit cutoff inclusive ( `T <= hts_Tcrit` ) and added a deck-side `critical temperature` key read in `MaterialFactory` ( `cl_MaterialFactory.cpp:283` ) that overrides what a plugin's `_init` set; the shipped deck carries `examples/tape_quench_usermat/input.conf:76` = `critical temperature : 90.0 K ;`. **Residue: (1) the long rerun** — 0.3 s ≈ 60 h serial at measured throughput, MPI ranks would shorten it; **(2) the np=8 residual floor** ( re-measured 2026-08-31 from the surviving logs; **the earlier "floor-collapse at step 264" framing was wrong and is retracted** ) — nothing happens at step 264. np=8 was pinned at the 1 µs floor essentially throughout: 229 floor-retry warnings over 264 accepted steps ( 87% ) against serial's 47 over 4191 ( 1.1% ), and serial sits at the floor at its OWN step 264 too ( Δt = 1.1 µs ). Step 264 is merely where 20 consecutive retries ran out ( `cl_FEM_Controller.cpp:2267` ). The real discriminator is the achievable magnetic residual at comparable Δt: serial descends 1.5e-3 → 4e-7 in 9 Picard iterations, while np=8 sits FLAT at 1.6-2.2e-4 through every iteration of all twenty retries — ~200× above the deck's 1e-6 tolerance, insensitive to both Δt and relaxation, i.e. a ~500× residual-floor difference from rank count alone. ( Full traces and the thermal machine-zero second symptom: `devlog/dl20260831_riva_n_guard_and_dr105_wall.md` §3. ) **One confound is now ELIMINATED, not merely doubted:** both runs used the identical binary ( Build Date `Aug 27 2026 03:25:34`, commit `dcca23cc`, DEBUG on, `hphiTrun` ), verified from both log headers, so "debug binary" is common-mode. Deck identity still rests on the 08-27 devlog's record rather than on the artifacts, because `belfem.log` does not echo solver settings. Mechanism remains UNDIAGNOSED — ranked hypotheses, not results: rank-dependent residual assembly / MPI-reduction inconsistency over owned-vs-ghost dofs ( 2e-4 is ~1e9× machine epsilon, far too large for roundoff on a 2133-node problem ), STRUMPACK's distributed factorization path, thin-shell / cut partitioning at 267 nodes per rank, and Anderson depth 3 ( ranked LOW — the residual is already 2e-4 at Picard iteration 1, before Anderson has history ). Bearing CLEARED as a suspect ( `link_bearings_with_dofs`, `cl_FEM_DofManager.cpp:1472`: broadcast table, every rank links wherever the node exists ). Cheap discriminators, ~15 min each since np=8 walls fast: `compute conditioning : true` at np=8 ( the abort message's own hint — a modest κ rules conditioning out and points at assembly ), np=8 with `library : mumps`, and np=2 / np=4 to see whether the floor scales with partition count or is a cliff. Log at `cmake-build-claude/tape_quench_dr105/belfem_np8_wall0375ms.log`; **(3) whether to soften the defect or the current table at all**, which is physics and Christian's call — and T4 already REFUTED the obvious lever there: the 175 A ramp-BC variant is a STRONGER drive, not a table-jump control. Compacted 2026-08-30; residue (2) re-measured and its framing retracted 2026-08-31 | **Two different decks share this row's name — do not confuse them.** The T5 envelope lives ONLY in the reconstructed `cmake-build-claude/tape_quench_dr105` ( strumpack/petsc, `anderson depth : 3`, timestep 0.002 → 0.02 ms ). The shipped `examples/tape_quench_usermat` is NOT the T5 deck and, as of 2026-08-31, is no longer even the same MODEL: it was rebuilt onto a single-tape mesh ( `tape.geo`, replacing the two-tape `tapestack3d.geo` ), moved to `resistivity type : riva`, sidesets 1,2 and one in/out curve pair, mumps at `:16`, timestep 0.5 → 1 ms at `:53-54`, still no Anderson. Running it discharges no residue of this row. **Two consequences for the row's diagnosis:** its `riva` selection removes the `n > 1` abort CLASS independently of the `critical temperature` remedy — `rho_riva` has no such precondition anywhere and `riva_rho_pl` takes n ≤ 1 as the finite ohmic limit `ec/jc` ( `powerlaws.hpp:2481-2512` ) — so this row now has two independent remedies where it records one; and its defect rewrite ( depth 0.25 → 0.1, re-sized against an explicit ~0.95 mm MPZ argument, switched on at 20 ms with a C2 quintic inside the current table's 5-155 ms plateau ) is residue (3)'s physics ruling TAKEN IN CODE, addressing both named drivers — though on the new model, not by softening the T5 deck. **CURRENTNESS 2026-08-31: the reconstructed deck is REPAIRED and residue (1) is unblocked.** It previously aborted at setup on its `custom { }` material blocks ( the factory hard-errors on that spelling since the 08-29 rename — a deliberate diagnostic, NOT an alias, `cl_MaterialFactory.cpp:252-262` ); all six material blocks now read `usermat` and `ybco1` carries `critical temperature : 90.0 K` ( `input.conf:66-72` ). It remains `resistivity type : piecewise`, so its quench-front protection is the `critical temperature` key alone. **The 2026-08-31 riva n-source gate is INERT for this deck** — every arm is conditioned on `ResistivityLaw::Riva` ( `Material::check_riva_n_source`, `cl_Material.cpp:790` ), so a piecewise deck takes none of them and nothing in that change can alter this run's behaviour. Serial evidence `belfem_serial.log`, parallel evidence `belfem_np8_wall0375ms.log`, both in the deck dir | no |
| DR-113 **[CODE]** **[P]**   | comm: **the point-to-point fabric has no structural defense against the DR-100 class.** Every exchange between a rank pair shares two tags ( `comm_tag` base for sizes/scalars, `+1` for payloads ), so correctness rests entirely on both ranks executing their exchanges in the same order — one skipped or surplus message silently poisons the NEXT recv on that tag ( truncate, garbage, or hang ), with the crash surfacing in whatever innocent collect runs later, an arbitrary distance from the producer. DR-100 took a month from defect to first observed crash and two probe campaigns to walk back to the producer. Architectural hardening options, in rising cost: ( a ) a debug-build drain check ( `MPI_Iprobe` sweep like the DR-100 probe ) at exchange-boundary chokepoints that aborts loudly at the FIRST stray, naming source/tag/size; ( b ) per-exchange tag separation ( widen `comm_tag` with an exchange-sequence component mod the tag limit ) so a stray can never match a later exchange's recv. The contract itself is now documented on `comm_tag()` ( 2026-08-27 ) | P3 | **option ( a ) landed 2026-08-31, gates owed** — `comm_drain_check()` in commtools ( barrier → `MPI_Iprobe` sweep of both pair tags → allreduced verdict → `BELFEM_ERROR` on EVERY rank, throw policy intact ) at five chokepoints: `DofManager::solve`, `solve_from_residual`, `postprocess`, `SolverData::collect_matrices` entry, `Controller::finalize` exit. No-op in release/serial; finalize is now collective in assert-active builds. Codex + Grok code audits accept at xhigh ( `tmp/ai_exchange/dr113_fabric_hardening.md` ). Gates: build + `make check` GREEN 2026-08-31, but that does **not** exercise this check — only `tests/comm` registers `TESTRANKS` ( 2 4 ) and that binary never builds a `DofManager`/`Controller`, while every other suite runs at one rank where the function returns at its `size() < 2` guard, so **no test reaches a chokepoint with more than one rank**. Still owed: a parallel gate deck ( RLC np=4 / 2D_Tapestack np=2, debug ) with zero drain aborts, and a deliberate-stray run proving the tripwire fires. Residuals: `comm_check` throw mid-sweep is not collective ( broken-MPI path ); a stray born inside `collect_matrices` before `collect_rhs_vector` is not covered. Option ( b ) tag separation stays deferred by design — touches every comm call site, `MPI_TAG_UB` care needed | any future stray now aborts at the nearest chokepoint naming source/tag/size; the check IS the archived DR-100 drain-probe pattern made permanent | no |
| DR-119 **[RUN]** **[P]**    | fem/kernel: **the coupled thermal watchdog clamp is landed and has never been seen to trip.** Before the fix, `watchdog_thermal()` needed `mIteration2 - mBestEpsilonIteration2 >= mWatchdogWindow2` ( window default 30, `cl_FEM_Controller.hpp:106-107` ), so on any deck whose magnetic `max iterations` ≤ the thermal window the magnetic ceiling ended the attempt first — measured at the DR-97 occurrence, a thermal best at iterate 3 put the trip at 33 against a ceiling that fired at 31. In fully-coupled mode the effective window is now clamped to `magnetic max iterations − thermal min iterations − 1`, floored at 1 ( `cl_FEM_Controller.cpp:4526-4559`, notice at `:4559` ); 0 stays the disable, segregated decks are untouched, and the clamp uses the deck snapshot plus the live member so floor escalation and `finalize` stay coherent. The setup notice is gated on rank 0 AND on the thermal section being present — **a magnetic-only deck defaults `mIsFullyCoupled = true` and must not be told about a thermal window** | P3 | open — **the only residue is the trip gate.** Implemented 2026-08-28 ( Christian chose the coupled-mode clamp over a default change or a warning ); both vendors approved plan and code; build + full test suite green ( Christian, 2026-08-29 ). **REVIEWED, NOT VERIFIED.** Both vendors jointly demoted the bound from "structural guarantee" to **conservative policy sized to an early thermal best**: the last watchdog-capable iterate is the magnetic ceiling itself ( `tMagneticDoomed` skips thermal there ), so on the measured deck 30→27 trips at exactly iterate 30, and a LATE best or the 10×-tolerance / growing-omega spare clauses can still let the ceiling win. Compacted 2026-08-30 | Grok's C2 spec: a run with magnetic `max iterations : 30` and `watchdog window` at its default must show the clamp notice ( needs InfoLevel::Default or louder ) AND a watchdog fire with ε2 above 10× the thermal tolerance and non-growing ω2 at the trip. Without both, a miss is a spare clause, not a clamp bug. **The gate deck must DEFAULT the thermal window:** the obvious in-tree candidate `examples/3D_tapestack` already sets `watchdog window : 8` on both fields ( `:31`, `:62` ) and cannot serve as-is | no |
| DR-122 **[CODE]** **[W]**   | homology: **order-2 cut-pipeline residue, split from DR-103 at strike time — three known gaps, all unreachable behind the deliberate order-1 hanging-edge guard** ( `BELFEM_ERROR( max_element_order() == 1 )` in `MaxwellFactory::create_hanging_edges_and_facets`, `cl_MaxwellFactory.cpp:1413` ): ( 1 ) the diagonal thick-cut cases ±5/6/7 rely on chord-midside neighbor coverage that `src/homology/doc/thick_thin_cuts_and_conjugate_edges.md` documents as unenforced; ( 2 ) `CutSet::create_duplicates` sets the duplicate's node source to the original unconditionally — no cascade when the original is itself hanging ( unlike `InterfaceProcessor::duplicate_nodes` and the ThinShellFactory ), which at order 2 trips the `is_hanging` assert at `cl_FEM_DofMgr_DofData.cpp:3513` in debug and writes a wrong T-matrix row in release; ( 3 ) no TRI6/TET10 homology fixture exists. NOT in scope: the former `fix weights for second order` todo — ruled stale at DR-103's strike ( unit nodal weights are exact at any Lagrange order ) | P3 | **open ( filed 2026-08-27 at DR-103's strike )** — watch only: no reachable trigger while the order-1 guard stands; flips to live work the day order-2 h-φ is scoped in | order-2 inductor mesh ( `scratchpad/dr31_order2/` ), once the order-1 guard is lifted | no — order-2 h-φ is not a 1.0 feature ( DR-103 ruling ) |
| DR-131 **[RUN]** **[P]**    | plugin templates on Darwin **— SCOPE WIDENED 2026-09-01: the templates were never the worse half.** Both `UserMaterialTemplate.cmake` and `UserLibraryTemplate.cmake` apply `-Wl,-undefined,dynamic_lookup` on `APPLE` and build `MODULE` rather than `SHARED`; that fix was raised by Codex and Grok in the 2026-08-28 jury round and had never been compiled on a Mac. It is now compiled, loaded and correct. What the gate found instead is that **the same fix was never applied to the four shipped example decks** — `examples/{disk_pulse,block3d,tape_quench_usermat,undulator2d}/src/CMakeLists.txt` still linked `SHARED` with no `dynamic_lookup`, so every deck's plugin failed to LINK on macOS, and each also forced `SUFFIX ".dylib"` on `APPLE` while its own `input.conf` names `src/build/usermat.so`. Two independent fatals per deck: no deck could build its plugin on a Mac, and none could have loaded it if it had. Widened here rather than branched, per the 2026-08-29 extend-don't-branch policy — same contract, same platform, same round | P2 | **GATE EXECUTED 2026-09-01 ( Claude, on Christian's Darwin machine — macOS 15.7.9 x86_64, gcc 15 + Apple clang ). The row's own suspicion is REFUTED and its by-catch is larger than the row.** Templates: 4/4 configure+build green ( source tree and a real `/tmp/belfem` prefix taking the installed branch, gcc and clang ), artifact is `lib<name>.so` as the docs claim, `nm` confirms the init symbols exported and 11 undefined `belfem::` symbols left for the host, `otool -L` confirms nothing linked. The `MODULE` change the row called 'the higher-risk half' is the half that works: **no Mac user's `.dylib` load path breaks, because on this tree no Mac user ever had a working one to break.** dlopen half: NOW RUN, not inferred — a host resolving against `libbelfem.dylib` loaded and evaluated the template plugin, and all five `tape_quench_usermat` materials incl. the `JcFunctionUserDefined` vtable case; then the real gate, `gmsh -3` + installed `belfem` on that deck, reached 295,315 dofs and the first BDF1 step, with a falsification control ( plugin hidden → abort at `Could not load library` ) proving the load path is on the run path. **Four decks FIXED the same session** ( `SHARED`→`MODULE`, `.dylib`/`.dll` suffix overrides dropped, `APPLE` link option added, `cmake_minimum_required` 3.13 for `target_link_options` ); 4/4 rebuild green and produce `.so`. ~~**Residual, and it is the honest one: no deck has been run to completion on Darwin, because that same run then died in `matvec_csc` — a defect unrelated to plugins, filed as DR-155.**~~ **RESIDUAL DISCHARGED 2026-09-01: a deck HAS now run to completion on Darwin.** Once DR-155's fix landed, a full Release + `USE_SHARED_LIBS=ON` build from current source was installed to a clean prefix and both remaining decks were run. `examples/tape_quench_usermat` cleared the step that used to kill it and then ran **54 timesteps ( 6 h 50 m, 0 crash signatures )** with five plugin-supplied materials across two `dlopen`ed libraries — stopped deliberately, not by failure, because that deck projects to ~114 h for its 0.3 s of stiff coupled quench. **`examples/disk_pulse` then ran to COMPLETION: 106 accepted timesteps, 0 rejected, t = 500.0000 ms ( its full `simulation time` ), 0 crash signatures in 3085 log lines, 100 Exodus frames written, process exited on its own.** Its `bgpulse.so` was confirmed MAPPED INTO THE LIVE PROCESS by `lsof` on the running pid, alongside the sandbox `libbelfem.0.9.0.dylib` — so the plugin was on the run path, not merely present on disk. That completion is what 54 `tape_quench` steps could not give: the **teardown path** — output finalisation and `dlclose` at exit. Both decks used the CMakeLists this row fixed, on the current source, with no `OMP_STACKSIZE` set. Retag `[RUN-BLOCKED]`→`[RUN]`: nothing blocks it now. **Strike is Christian's ruling, not taken here** | EXECUTED — both templates and all four decks on macOS 15.7.9 against `/tmp/belfem`; `examples/tape_quench_usermat` end-to-end with the falsification control. `devlog/dl20260901_darwin_plugin_gate.md`; **DISCHARGED** — `examples/disk_pulse` to completion on Darwin ( 106 steps, t = 500 ms, 100 frames, clean exit, plugin `lsof`-confirmed on the run path ) | no |
| DR-133 **[CODE]** **[cdP]** | homology 2-D cut orientation: **`CutFactory::orient_terminal_curves_2D` is geometrically vacuous — its "does this curve run counter-clockwise" test carries no information about which side the loop encloses.** It builds `tB` as the in-plane 90-degree rotation of the tape facet tangent and then `tR = z x tB`, which reduces *identically* to `-(facet tangent)` for every input; no enclosed-side vector, interior point or outward normal ever participates. The test therefore degenerates to "orient the curve antiparallel to the tape facet's node ordering", i.e. it inherits whatever the facet master/slave assignment happens to be rather than deciding anything geometrically. The 3-D sibling `orient_terminal_curves` is a real test — it crosses two independent surface normals (`cl_CutFactory.cpp:1819-1917` vs `:1920-2000`). Confirmed independently by Codex in the 2026-08-28 sign round. Benign in practice **today**: facet master/slave is BFS-deterministic from cross-type seeds (`src/fem/maxwell/doc/thin_shell_facet_orientation.md`), and the 2026-08-28 gantry gate measured a consistent orientation across all 464 tapes | P2 | open — filed 2026-08-28 as by-catch of the 2-D current-sign repair (DR-133 is NOT that defect; the sign fix landed in `Homology::reorient_generators` and is gate-verified). Deliberately not touched in the same change: repairing a vacuous orientation test and flipping a sign convention in one diff would have confounded the discriminator that selected the fix. Open question is whether a mesh whose thin-shell facet ordering differs from the gantry's can flip an individual tape's polarity — not demonstrated, not excluded | build a 2-D thin-shell deck with tape sidesets deliberately ordered against the gantry's convention (or a mesh with reversed block ordering feeding the master/slave BFS) and measure per-tape current sign: all tapes must match their declared sign, as the gantry gate showed 464/464 | no — no shipped deck exhibits it |
| DR-155 **[RUN]** **[P]**    | sparse/OpenMP: **`matvec_csc`'s `!$omp reduction(+:y)` puts each worker thread's private copy of the WHOLE result vector on that thread's stack** ( `src/sparse/splinalg.f90:103`; `y` is an explicit-shape dummy at `:77`, so gfortran materialises the private copy in the thread frame ). libgomp gives a Darwin worker 2 MiB, so a multithreaded CSC product dies with SIGBUS/SIGSEGV once `n * 8` bytes crosses it. The kernel comment at `:94-99` already prices the scratch at 'n * 8 bytes per thread' — it never says the bytes are stack, and nothing checks them against the limit. The only OpenMP array reduction in the Fortran tree. **SCOPE WIDENED 2026-09-01 ( Christian's ruling: one DR-155 sweep, not a new row ) after a Codex+Grok jury round on the plan. Three of the row's own claims were WRONG and are corrected here.** **(1) `USE_MKL=ON` does NOT bypass the kernel.** That guard covers only the 5-argument `SpMatrix::multiply`; the 2-argument overload ( `cl_SpMatrix.cpp:1737-1774` ) has no `BELFEM_MKL` branch at all and calls `matvec_csc` unconditionally at `:1747`, so **MKL builds crash on the eigenvalue path** ( `cl_FEM_DofMgr_EigenValues.cpp:1670,1706,1712,1732-1733,1755-1759`, inverse iteration ). Raised independently four times. The same overload should have been using MKL all along — routing it through the 5-arg sibling gains MKL, gains the two dimension asserts the 2-arg overload lacks ( `:1633`, `:1638` ), and deletes one of the two `matvec_csc` call sites; but it must `fill( 0.0 )` first, because the Fortran kernel zeroes `y` itself and callers pass an UNINITIALISED `aY` ( `mZreal.set_size( mN )`, `cl_FEM_DofMgr_EigenValues.cpp:1661` ), while `mkl_sparse_d_mv` is not documented to short-circuit beta == 0 and `0.0 * NaN` is NaN. **(2) The Linux cliff figure is UNEARNED.** '8 MiB → 1,048,576 rows' was assumed; two incompatible readings now exist ( libgomp may be SETTING 2 MiB, which would put Linux at the SAME 262,144 — macOS's pthread default is 512 KiB, so something set 2 MiB; or the worker stack follows `RLIMIT_STACK` and is site policy, possibly BELOW Darwin's ). **(3) 'Measured to the byte' overstates the sweep:** the boundary is bracketed to ( 262,000, 262,144 ], a 1152-byte window; no point between was run. **The fix as planned was NOT approved by either auditor** — the draft R1 replaced the reduction with an `allocate`d buffer without replacing the identity-initialisation the reduction was silently providing, which produces SILENTLY WRONG matvecs rather than a crash ( plus sizing from `omp_get_max_threads()`, the cap rather than the team, leaving unwritten columns in the sum ). Also found: no Fortran abort primitive exists in `src/sparse/*.f90` so the `stat=` branch has to be invented; `mSwap` is a bad precedent twice over ( `BELFEM_NETLIB`-only, `cl_SpMatrix.hpp:85-88`, and LIVE across the kernel whenever beta != 0, which is the residual path that crashed ); and **two documents assert this defect is impossible** — `doc/coding_philosophy.md:674` ( 'compiled BELFEM code contains ZERO OpenMP pragmas and spawns no threads' ) and `doc/parallel_execution.md:20-22` ( '`OMP_NUM_THREADS` accelerates only the third-party numerics' ), both false against 19 `!$omp` lines in three Fortran files and a `gomp_thread_start` crash frame, and both missed by `check_doc_claims.py` at 37/37 | P2 | **FIXED IN THE WORKING TREE 2026-09-01 by REMOVING THE PARALLELISM, NOT REPAIRING IT ( Christian's ruling ) — NOT YET COMPILED OR TESTED.** The turn came from a question none of the three reviewers asked: what does this threading actually BUY? Both matvecs are MASTER-ONLY ( `cl_FEM_DofMgr_SolverData.cpp:2308` guards the residual at `:2368`; `cl_FEM_DofMgr_EigenValues.cpp:1704` guards the eigen pair ), so OpenMP was their only parallelism — but at ~2 flops per nonzero they are noise beside a full assembly and a direct factorization, and **no threaded-vs-serial measurement exists anywhere in the tree** ( the kernel comment compares atomics against the reduction, two THREADED variants ). So a defect whose repair needed a buffer registry, a two-role split, hand-written reduction semantics and a 512 MiB scratch budget was closed with a default-OFF build token. **New `BELFEM_OMP` define, distinct from `OMP`:** `USE_BELFEM_OPENMP` ( default OFF, `CMakeLists.txt:88` ), define appended under `USE_OPENMP AND USE_BELFEM_OPENMP` ( `:179-181` ); all 12 `!$omp` blocks in `splinalg.f90`, `arpacktools.f90`, `parpacktools.f90` wrapped in `#ifdef BELFEM_OMP`, the four ARPACK `use omp_lib` guards moved, and `splinalg.f90`'s two subroutine headers given the same guarded import. **NINE `#ifdef OMP` sites deliberately did NOT move** — a blanket sweep would have silently deleted the oversubscription warning ( `hatch_turtle()` ), serialised PARDISO ( `gParameters(3)` ) and hidden the banner's thread budget, all on the default build; those three now carry explicit 'NOT a mistake' comments. A Darwin-only ban was argued against and dropped: O2 unresolved means a carve-out could leave the same cliff live on Linux, and platform-conditional threading gives platform-divergent rounding. **This does NOT repair the kernel — `USE_BELFEM_OPENMP=ON` re-arms DR-155**, and the process is still multi-threaded via STRUMPACK/MKL/BLAS, so `OMP_STACKSIZE` remains a live knob; both facts documented in `doc/parallel_execution.md`. Side benefit: `matvec_csc` is now bitwise deterministic across thread counts. **BUILD GATE, CONFIGURATION 1 of 3, RAN GREEN 2026-09-01 ( Christian's build, Darwin debug `USE_BELFEM_OPENMP=OFF` / `USE_MKL=OFF` / Blaze ): `make check` 15/15, 0 failed.** The fix is CONFIRMED PRESENT IN THE BINARY rather than inferred from the suite result — `nm` on the built `splinalg.f90.o` shows ZERO OpenMP symbols, and the falsification control on the same source compiled `-DBELFEM_OMP -fopenmp` yields `_matvec_csc._omp_fn.0` plus `_GOMP_parallel` / `_GOMP_atomic_*`, which is the LITERAL faulting frame of the original crash report. That control also shows the token is a real switch, not a one-way deletion. **What the green does NOT establish:** the existing thread-count test `tests/sparse/test_SpMatrix.cpp:1782` now passes TRIVIALLY ( serial kernel, so its 1/2/8 sweep is identical by construction ) and cannot exercise the defect until the token is ON; the build was DEBUG only; and the MKL forwarder is untouched by it. **Other executed gates:** preprocessor ( 11 `!$omp` lines with the define, 0 without ) and `gfortran -fsyntax-only` green on all three kernels in BOTH token states ( 6/6 ). **OWED:** build + `make check` in both token states, and the `tape_quench_usermat` deck on a default environment. **The MKL half is FIXED too ( Christian: 'SpMatrix::multiply should use MKL if MKL is available' ):** the 2-argument overload no longer switches on `mType` and calls the Fortran kernels — it forwards to the 5-argument one as `multiply( aX, aY, 1.0, 0.0, false )`, which carries the `BELFEM_MKL` branch. MKL builds now use `mkl_sparse_d_mv` on the ARPACK inverse-iteration path, the overload gains the two dimension asserts it never had, and **both remaining `matvec_csc`/`matvec_csr` call sites are now inside the `#else` of `BELFEM_MKL`** — so the row's ORIGINALLY FALSE scope guard is true as an end state. The forward is instruction-for-instruction equivalent on the Fortran branch ( beta = 0 skips the `mSwap` save and the add-back, alpha = 1 skips the scaling, untransposed reduces the duality selection to the old switch ). The `aY.fill( 0.0 )` is `#ifdef BELFEM_MKL` ONLY and that asymmetry is deliberate: the Fortran kernels zero `y` themselves so callers may pass an UNINITIALISED `aY` ( the eigen driver does ), `mkl_sparse_d_mv` is not documented to short-circuit beta == 0, and `0.0 * NaN` is NaN — while on the Fortran branch an unconditional fill would be a second pointless O(n) pass. **The MKL branch is UNVERIFIABLE on Darwin** ( `config_mkl.cmake:2-3` is a hard configure error on APPLE ), so the one path this change exists to enable is the one that cannot be compiled where it was written — a Linux `-DUSE_MKL=ON` build is owed. O2 demoted from a severity question to a precondition for ever re-arming the token | build + `make check` at `-DUSE_BELFEM_OPENMP=OFF` **and** `=ON` ( the ON build proves the token is a real switch, not a one-way deletion; DR-155 returns there above 262,144 rows without `OMP_STACKSIZE`, which is the discrimination ); end-to-end `examples/tape_quench_usermat` on Darwin with NO `OMP_STACKSIZE` set; **plus a Linux `-DUSE_MKL=ON` build exercising an eigenvalue case**, the only way to compile and run the forwarded MKL path | no |
| DR-156 **[RUN]** **[P]**    | sparse/graph: **`reordering scheme : parmetis` / `ptscotch` are wired ( PETSc path, `DistMatrix::order_graph`, 2026-09-04 ) and green under `sparsempi_np2/_np4`, but NEVER BENCHMARKED.** The wiring's own plan ( `todo/closed/parmetis_ptscotch_wiring.md`, W-R5 ) says an ordering that is not faster than serial METIS on rank 0 is not worth the dependency, and no measurement exists: root still builds and holds the complete graph, so this parallelises the ordering COMPUTE after a root-side scatter, not memory. By-catch of the same session, fixed and gated: **D5** — the matrix graph's diagonal self-loops corrupted `METIS_NodeNDP`'s heap on today's PETSc+`metis` path ( latent, no deck used it ), now dropped in both adjacency builders | P2 | wired, tested at np 2/4, benchmark OWED | a PETSc deck at np ≥ 4 ( tapestack3d or the cheapest that reaches `DistMatrix` ) run with `reordering scheme : metis`, `parmetis`, `ptscotch`: ordering time and total solve wall time; keep or strike the two values on the numbers | no |
| DR-157 **[MIXED]** **[P]**  | ci/tests: **the nightly GitLab job never compiles the gas-model tests.** All four matrix jobs in `.gitlab-ci.yml` configure with `-DUSE_TEST=ON -DUSE_DEBUG=… -DUSE_MATRIX_…` and no `-DUSE_GASMODELS=ON`, so `test_gastables` and `test_gasmodels` never exist as targets and the `if( TARGET )` guard in the `check` fan-out ( `CMakeLists.txt` ) skips them silently. Those two files carry about 4,650 of the ~4,975 physics test lines ( `tests/physics/CMakeLists.txt` gates them on `USE_GASMODELS`, default OFF ), so default-build physics coverage is ~0.7% and the nightly has never run the gas tables or gas models. Christian ruled on 2026-08-30 that the nightly configures the option; the ruling was recorded as resolved in the test-hardening plan but the job definition was never changed. Their runtime data lives in `share/fluid` and is reached through the `BELFEM_DATA` property `Add_Test.cmake` already sets, so no fixture plumbing is owed | P2 | one token owed in `.gitlab-ci.yml` ( Christian's infrastructure ), then observe a scheduled pipeline whose ctest log lists the `gastables` and `gasmodels` suites; order: edit, run, strike | the next scheduled pipeline after the flag lands: the ctest log under `ctest-logs/` must name `gastables` and `gasmodels` among the executed suites in all four matrix jobs | no |
| DR-158 **[CODE]** **[W]**   | build/tests: **the Tier 2 MPI launcher is never checked to be the MPI the binary was linked against.** `config/system/find_mpi.cmake` identifies the MPI flavor by preprocessing the *compiler's* `mpi.h` into `BELFEM_MPI_FLAVOR`; `config/scripts/Add_Test.cmake` then picks `mpirun`/`mpiexec` with `find_program` from `${BELFEM_MPIHOME}/bin`, `$ENV{MPI_HOME}/bin` and finally PATH, and reuses the compiler-side flavor verdict for the `--oversubscribe` decision. With `MPI_HOME` unset, a PATH launcher from a different installation ( a second Open MPI, or MPICH on a mixed host ) can launch a binary linked against another MPI. The failure mode is a launch crash or a job that never forms, never a silent green: the rank sentinel and the MAX-reduced verdict catch a singleton launch. Accepted as a known limit when Tier 2 landed on 2026-08-30 and recorded only in the plan that has since been removed | P3 | latent; no current trigger ( every configured tree so far has one MPI on PATH ). Fix, if wanted: run `${BELFEM_MPIEXEC} --version` at configure and refuse a launcher whose banner does not match `BELFEM_MPI_FLAVOR`, overridable like `ALLOW_UNTESTED_MPI` | configure a tree with `MPI_HOME` unset and a foreign `mpirun` first on PATH; `make check` must fail at configure with a named message rather than at launch | no |
